/*	
// example
var listTemplateExtraInfo = {
	"columnModel" : {

	},
	"queryParameter" : {
		"property" : {
			listeners : {
				change : function(newValue, oldValue, formObj) {// 值变化, formObj 当前这个element对应的LTextField,LSelectField,......
				}
			},
			selectFunc: function(selectValueLi, formObj) {// ,弹窗点确定按钮后的回调,没有返回值
			},
			unSelectFunc: function(formObj) {// ,点击删除按钮触发,没有返回值
			},
			queryFunc: function(formObj) {// ,return {},开窗回选函数,
				return {};
			}
		}
	}
};
*/

var listTemplateExtraInfo = {
	"columnModel" : {

	},
	"queryParameter" : {
		
	}
};

function main() {
	
}
